/**
 * Fixture with an `app` export that is not a Tsadwyn instance. Used to exercise
 * the CLI's "does not have generateAndIncludeVersionedRouters" error branch.
 */
export const app = { notATsadwyn: true };
