/**
 * Fixture with an `app` export that is not a Cadwyn instance. Used to exercise
 * the CLI's "does not have generateAndIncludeVersionedRouters" error branch.
 */
export const app = { notACadwyn: true };
